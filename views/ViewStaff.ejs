<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>View Staff</title>

  <!-- Bootstrap & FontAwesome -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://kit.fontawesome.com/a2e0e6ad7e.js" crossorigin="anonymous"></script>

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700&display=swap" rel="stylesheet">

  <style>
    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(to right, #1f1c2c, #928dab);
      color:black;
      padding: 40px 20px;
      animation: fadeIn 0.8s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .container {
      max-width: 1100px;
      margin: auto;
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(12px);
      padding: 30px 40px;
      border-radius: 18px;
      box-shadow: 0 15px 30px rgba(0, 0, 0, 0.25),
                  0 0 40px rgba(170, 140, 255, 0.1);
    }

    h1 {
      text-align: center;
      font-weight: 700;
      color:black;
      margin-bottom: 25px;
      font-size: 2.5rem;
      text-shadow: 0 0 10px rgba(255, 193, 7, 0.4);
    }

    .search-bar {
      text-align: center;
      margin-bottom: 30px;
    }

    #searchInput {
      width: 100%;
      max-width: 400px;
      padding: 12px 16px;
      border-radius: 12px;
      border: none;
      background-color: #2e2b4f;
      color: #fff;
      font-size: 15px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.3);
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 10px;
      font-size: 15px;
      background: rgba(255,255,255,0.04);
      border-radius: 14px;
      overflow: hidden;
    }

    thead {
      background: black;
      color:white;
    }

    thead th {
      padding: 14px;
      text-align: center;
      font-weight: bold;
    }

    tbody td {
      padding: 14px;
      text-align: center;
      border-bottom: 1px solid rgba(255, 255, 255, 0.05);
      color:black;
    }

    tbody tr:hover {
      background-color: rgba(255, 255, 255, 0.07);
    }

    .action-icons i {
      font-size: 18px;
      margin: 0 6px;
      cursor: pointer;
      transition: 0.3s ease;
    }

    .fa-pen-to-square {
      color: #ffc107;
    }

    .fa-trash {
      color:brown;
    }

    .fa-pen-to-square:hover {
      color:aqua ;
      transform: scale(1.2);
    }

    .fa-trash:hover {
      color: #ff1744;
      transform: scale(1.2);
    }

    .no-data {
      text-align: center;
      padding: 20px;
      font-style: italic;
      color: #ccc;
    }

    @media (max-width: 768px) {
      thead th, tbody td {
        font-size: 13px;
        padding: 10px;
      }
    }
  </style>
</head>
<body>

  <div class="container">
    <h1>👨‍🍳 Staff List</h1>

    <div class="search-bar">
      <input type="text" id="searchInput" placeholder="Search staff..." onkeyup="searchstaff(this.value)">
    </div>

    <div class="table-responsive">
      <table>
        <thead>
          <tr>
            <th>Sr</th>
            <th>Name</th>
            <th>Email</th>
            <th>Contact</th>
            <th>Salary</th>
            <th>Password</th>
            <th>Update</th>
            <th>Delete</th>
          </tr>
        </thead>
        <tbody id="tbody">
          <% if (data.length === 0) { %>
            <tr>
              <td colspan="8" class="no-data">No staff found. Please add new staff.</td>
            </tr>
          <% } else { %>
            <% data.forEach((item, index) => { %>
              <tr>
                <td><%= index + 1 %></td>
                <td><%= item.name %></td>
                <td><%= item.email %></td>
                <td><%= item.contact_no %></td>
                <td>₹<%= item.salary %></td>
                <td><%= item.password %></td>
                <td class="action-icons">
                  <a href="/updatestaff?id=<%= item.staff_id %>"><i class="fa-solid fa-pen-to-square"></i></a>
                </td>
                <td class="action-icons">
                  <a href="/Deletestaff?id=<%= item.staff_id %>"><i class="fa-solid fa-trash"></i></a>
                </td>
              </tr>
            <% }); %>
          <% } %>
        </tbody>
      </table>
    </div>
  </div>

  <script src="/searchstaff.js"></script>
</body>
</html>
