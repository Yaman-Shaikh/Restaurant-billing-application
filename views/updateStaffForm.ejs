<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Update Staff Info</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Bootstrap + FontAwesome -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <script src="https://kit.fontawesome.com/a2e0e6ad7e.js" crossorigin="anonymous"></script>

  <style>
    body {
      background: white; /* Black background */
      font-family: 'Segoe UI', sans-serif;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 30px;
    }

    .form-container {
      width: 100%;
      max-width: 700px;
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(10px);
      border-radius: 20px;
      padding: 40px 30px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
      animation: fadeIn 0.7s ease-in-out;
      color: #fff;
      border: 1px solid rgba(255, 255, 255, 0.08);
    }

    @keyframes fadeIn {
      0% {
        opacity: 0;
        transform: translateY(40px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .form-title {
      font-size: 30px;
      font-weight: 600;
      color:black;
      text-align: center;
      margin-bottom: 25px;
      border-bottom: 2px solid #9c27b0;
      padding-bottom: 10px;
    }

    .form-label {
      font-weight: 500;
      color: black;
    }

    .form-control {
      border-radius: 12px;
      padding: 12px 15px;
      font-size: 15px;
      background-color: rgba(255, 255, 255, 0.07);
      color: black;
      border: 1px solid #444;
    }

    .form-control:focus {
      border-color: #bb86fc;
      box-shadow: 0 0 0 4px rgba(187, 134, 252, 0.2);
      background-color: rgba(255, 255, 255, 0.1);
    }

    .btn-gradient {
      background: linear-gradient(to right, #ffd700, #9c27b0); /* Yellow to Purple */
      border: none;
      font-size: 16px;
      padding: 12px 30px;
      border-radius: 30px;
      font-weight: 600;
      color: #000;
      transition: all 0.3s ease;
    }

    .btn-gradient:hover {
      background: linear-gradient(to right, #fdd835, #7b1fa2);
      box-shadow: 0 6px 12px rgba(255, 215, 0, 0.4);
      color: #fff;
    }

    .btn-secondary {
      font-size: 16px;
      padding: 12px 30px;
      border-radius: 30px;
      background-color: #444;
      color: #fff;
      border: none;
      transition: 0.3s;
    }

    .btn-secondary:hover {
      background-color: #666;
      box-shadow: 0 6px 12px rgba(255, 255, 255, 0.1);
    }

    @media (max-width: 576px) {
      .form-container {
        padding: 30px 20px;
      }

      .btn-gradient, .btn-secondary {
        width: 100%;
        margin-bottom: 10px;
      }
    }
  </style>
</head>

<body>
  <div class="form-container">
    <h2 class="form-title"><i class="fas fa-user-edit me-2"></i>Update Staff Info</h2>
    <form action="/UpdateStaff" method="POST">
      <input type="hidden" name="id" value="<%= staff.staff_id %>" />

      <div class="mb-3">
        <label class="form-label">Name</label>
        <input type="text" class="form-control" name="name" value="<%= staff.name %>" required />
      </div>

      <div class="mb-3">
        <label class="form-label">Email</label>
        <input type="email" class="form-control" name="email" value="<%= staff.email %>" required />
      </div>

      <div class="mb-3">
        <label class="form-label">Contact</label>
        <input type="text" class="form-control" name="contact_no" value="<%= staff.contact_no %>" required />
      </div>

      <div class="mb-3">
        <label class="form-label">Salary</label>
        <input type="number" class="form-control" step="0.01" name="salary" value="<%= staff.salary %>" required />
      </div>

      <div class="mb-3">
        <label class="form-label">Password</label>
        <input type="text" class="form-control" name="password" value="<%= staff.password %>" required />
      </div>

      <div class="text-center mt-4">
        <button type="submit" class="btn btn-gradient me-2">
          <i class="fas fa-save me-1"></i>Update
        </button>
        <a href="/stafflist" class="btn btn-secondary">
          <i class="fas fa-times-circle me-1"></i>Cancel
        </a>
      </div>
    </form>
  </div>
</body>
</html>
